# Web Framework
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-CORS==4.0.0

# Database
SQLAlchemy==2.0.23

# Web Scraping (existing scraper dependencies)
beautifulsoup4==4.12.2
requests==2.31.0
googlesearch-python==1.2.4
# lxml==4.9.3  # Optional: Only needed for the existing Google Maps scraper
# On Windows, if lxml fails to install, you can either:
#   1. Skip it (Outbound Omega v2 doesn't need it)
#   2. Install pre-built wheel: pip install lxml
#   3. Use html5lib instead: pip install html5lib

# Environment & Utilities
python-dotenv==1.0.0
